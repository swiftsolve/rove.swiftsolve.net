{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "rove-dev",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        ". \"${NVM_DIR:-$HOME/.nvm}/nvm.sh\" >/dev/null 2>&1 && nvm use >/dev/null 2>&1; exec npm run dev -- --port 3000"
      ],
      "port": 3000
    },
    {
      "name": "rove-dev-preview",
      "runtimeExecutable": "bash",
      "runtimeArgs": [
        "-c",
        ". \"${NVM_DIR:-$HOME/.nvm}/nvm.sh\" >/dev/null 2>&1 && nvm use >/dev/null 2>&1; exec npm run dev"
      ],
      "autoPort": true
    }
  ]
}
